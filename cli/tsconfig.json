{
  // 编译选项,可以被忽略，这时编译器会使用默认值
  "compilerOptions": {
    // 目标代码类型
    "target": "es5",
    // 指定生成哪个模块系统代码
    "module": "commonjs",
    // 生成相应的.d.ts文件
    "declaration": true,
    // 编译后结果所在目录
    "outDir": "build"
  }
}